version: "3"

services:
  mariadb:
    image: mariadb
    container_name: mariadb
    build:
      context: requirements/MariaDB
      dockerfile: Dockerfile
    ports:
      - "3001:3306"
    volumes:
      - mariadb_data:/var/lib/mysql
    env_file:
      - ./.env
    networks:
      - inception
    restart: always

  nginx:
    image: nginx
    container_name: nginx
    build:
      context: requirements/Nginx
      dockerfile: Dockerfile
    depends_on:
      - wordpress
    ports:
      - "443:443"
    volumes:
      - wordpress_data:/var/www/html
    env_file:
      - ./.env
    networks:
      - inception
    restart: always

  wordpress:
    image: wordpress
    container_name: wordpress
    build:
      context: requirements/WordPress
      dockerfile: Dockerfile
    depends_on:
      - mariadb
    env_file:
      - ./.env
    volumes:
      - wordpress_data:/var/www/html
    networks:
      - inception
    restart: always

volumes:
  mariadb_data:
    driver_opts:
      type: none
      o: bind
      device: /home/aachhoub/Desktop/inception/data/mariadb_data
  wordpress_data:
    driver_opts:
      type: none
      o: bind
      device: /home/aachhoub/Desktop/inception/data/wordpress_data

networks:
  inception:
    driver: bridge